/**
 * Spark & Drive Parts Finder — AI-assisted external search, refined for clarity.
 * Confidence tiers never imply OEM certification or guaranteed fit.
 */

/** Official product name for UI. */
export const PARTS_FINDER_PRODUCT_NAME = "Spark Parts Finder";

/** Hero / positioning — VIN/chassis/vehicle context, OEM or equivalent matches, results with images and summaries. */
export const PARTS_FINDER_HERO_LINE =
  "Use the search engine to find evidence-based matches for your exact OEM number or high-quality equivalent parts, based on your VIN, chassis, or vehicle details, with images and product summaries included.";

/** Supporting line: how we work (external signals, ranking, user-safe output). */
export const PARTS_FINDER_ENGINE_PROMISE =
  "We interpret your vehicle and part intent, gather web evidence, rank candidates for fit and quality signals, and return a short curated list—never a raw dump of unvetted links.";

/** Secondary benefit line (results surface). */
export const PARTS_FINDER_RESULTS_PROMISE =
  "Honest confidence tiers, top matches first, and clear next steps for sourcing or support.";

/** Short nav label (sidebar / mobile). */
export const PARTS_FINDER_NAV_LABEL = "Parts Finder";

/** Compact card hook on dashboard overview. */
export const PARTS_FINDER_CARD_HOOK =
  "Web-aware search — refined matches, never raw link dumps.";
